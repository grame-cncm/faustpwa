# Faust deployed Progressive Web Applications

## Build Instructions

Clone this repository, then run:

```shell
make install
```

Build the site:

```shell
make build
```

Test the site locally:

```shell
make serve
```
